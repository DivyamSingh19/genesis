import prisma from "../utils/prisma"
//logic being we set a cookie for the prompts that the user gives once the users selects a particular model we update the cookie considering that the user might 
//might use the particular model for a set amount of prompts till the credits expire
 

export const changeModel= async(req:Request,res:Response)=> {
    try {
        //steps:1. get userId from the middleware
        //check 
        
    } catch (error) {
        
    }
}

